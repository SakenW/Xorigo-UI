# NPM 认证配置
# 注意：认证令牌应通过环境变量设置，不要直接写入文件
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# 基础配置
registry=https://registry.npmjs.org/

# 发布配置
access=public
tag=latest

# 安全配置
audit-level=high
fund=false

# 缓存配置
cache=/tmp/npm-cache

# 工作区配置
workspaces=true
legacy-peer-deps=false