/**
 * FieldLabel组件模块
 *
 * @packageDocumentation
 */

/**
 * FieldLabel组件 - 表单字段标签组件
 *
 * 提供完整的表单字段标签功能，包括：
 * - 必填标记显示
 * - 可选标记显示
 * - 隐藏标签（屏幕阅读器可见）
 * - 图标集成
 * - 自定义样式
 * - 禁用状态
 * - 点击聚焦关联输入框
 * - HTML标签可配置
 * - 描述文本
 * - TypeScript 类型安全
 * - React Context 集成
 * - 可访问性支持
 */
export { FieldLabel, FieldLabelDescription } from './field-label';

/**
 * FieldLabel组件的属性类型
 */
export type { FieldLabelProps } from './field-label';

/**
 * FieldLabelDescription组件的属性类型
 */
export type { FieldLabelDescriptionProps } from './field-label';
