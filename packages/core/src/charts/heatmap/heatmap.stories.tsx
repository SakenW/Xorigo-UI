/**
 * @fileoverview Heatmap 组件 Storybook 故事
 * @description 展示热力图组件的各种使用场景和配置选项
 *
 * 故事列表：
 * 1. Default - 默认基础使用
 * 2. WithValues - 显示数值标签
 * 3. CustomStyle - 自定义样式配置
 * 4. NoGrid - 无网格线样式
 * 5. NoLegend - 无图例样式
 * 6. NoTooltip - 无工具提示样式
 * 7. CustomColors - 自定义颜色方案
 * 8. CustomAnimation - 自定义动画配置
 * 9. LargeDataset - 大数据集展示
 * 10. MultiSeries - 多系列数据
 * 11. NegativeValues - 负值数据
 * 12. WithThemeBridge - 主题系统集成
 * 13. Small - 小尺寸示例
 * 14. Large - 大尺寸示例
 * 15. PercentageWidth - 百分比宽度示例
 *
 * @author Xorigo UI Team
 * @version 1.0.0
 * @since 2025-11-04
 */

import type { Meta, StoryObj } from '@storybook/react'
import { Heatmap, type HeatmapSeries } from './heatmap'
import { ThemeBridge } from '../../theme'

// ============================================================================
// Stories Metadata
// ============================================================================

/**
 * Storybook 元数据配置
 * 定义组件的基本信息、参数和文档说明
 */
const meta = {
  /**
   * 组件在 Storybook 中的路径
   * 按组件分类组织：Charts/Heatmap
   */
  title: 'Charts/Heatmap',

  /**
   * 关联的组件
   * 指向 Heatmap 组件
   */
  component: Heatmap,

  /**
   * 故事布局配置
   */
  parameters: {
    /**
     * 布局方式 - 居中显示
     */
    layout: 'centered',

    /**
     * 文档配置
     */
    docs: {
      description: {
        component: `
热力图组件用于数据可视化，通过颜色深浅表示数值大小。
支持矩阵数据展示、颜色映射、悬停交互、图例显示等功能。
适用于展示二维数据的分布和关联关系，如销售数据、用户行为等。
        `.trim()
      }
    }
  },

  /**
   * 自动生成文档
   * 包含组件 Props 和故事的文档
   */
  tags: ['autodocs'],

  /**
   * 参数类型定义
   * 定义每个 prop 的控件和文档
   */
  argTypes: {
    /**
     * 热力图数据
     * 控制方式：对象输入
     * 描述：包含系列名称和数据点的数组
     */
    data: {
      control: 'object',
      description: '热力图数据',
      table: {
        type: { summary: 'HeatmapSeries[]' }
      }
    },

    /**
     * 是否显示数值标签
     * 控制方式：布尔开关
     * 描述：在每个单元格中显示具体数值
     */
    showValues: {
      control: 'boolean',
      description: '是否显示数值标签',
      table: {
        type: { summary: 'boolean' },
        defaultValue: { summary: 'false' }
      }
    },

    /**
     * 是否显示工具提示
     * 控制方式：布尔开关
     * 描述：鼠标悬停时显示详细信息的提示框
     */
    showTooltip: {
      control: 'boolean',
      description: '是否显示工具提示',
      table: {
        type: { summary: 'boolean' },
        defaultValue: { summary: 'true' }
      }
    },

    /**
     * 是否显示图例
     * 控制方式：布尔开关
     * 描述：在底部显示颜色映射图例
     */
    showLegend: {
      control: 'boolean',
      description: '是否显示图例',
      table: {
        type: { summary: 'boolean' },
        defaultValue: { summary: 'true' }
      }
    },

    /**
     * 是否显示网格线
     * 控制方式：布尔开关
     * 描述：在单元格之间显示网格线
     */
    showGrid: {
      control: 'boolean',
      description: '是否显示网格线',
      table: {
        type: { summary: 'boolean' },
        defaultValue: { summary: 'true' }
      }
    },

    /**
     * 单元格间距
     * 控制方式：数字滑块（0-20）
     * 描述：相邻单元格之间的像素间距
     */
    cellGap: {
      control: { type: 'number', min: 0, max: 20 },
      description: '单元格间距',
      table: {
        type: { summary: 'number' },
        defaultValue: { summary: '2' }
      }
    },

    /**
     * 单元格圆角
     * 控制方式：数字滑块（0-10）
     * 描述：单元格矩形的圆角半径
     */
    cellRadius: {
      control: { type: 'number', min: 0, max: 10 },
      description: '单元格圆角',
      table: {
        type: { summary: 'number' },
        defaultValue: { summary: '4' }
      }
    },

    /**
     * 颜色调色板
     * 控制方式：对象输入
     * 描述：用于数值映射的颜色数组
     */
    colorScale: {
      control: 'object',
      description: '颜色调色板',
      table: {
        type: { summary: 'string[]' }
      }
    },

    /**
     * 动画持续时间
     * 控制方式：数字滑块（0-5 秒）
     * 描述：组件加载动画的持续时间
     */
    animationDuration: {
      control: { type: 'number', min: 0, max: 5 },
      description: '动画持续时间（秒）',
      table: {
        type: { summary: 'number' },
        defaultValue: { summary: '1.2' }
      }
    },

    /**
     * 图表宽度
     * 控制方式：文本输入
     * 描述：图表的宽度，支持数值或百分比
     */
    width: {
      control: { type: 'text' },
      description: '图表宽度',
      table: {
        type: { summary: 'number | string' },
        defaultValue: { summary: '100%' }
      }
    },

    /**
     * 图表高度
     * 控制方式：文本输入
     * 描述：图表的高度，支持数值或百分比
     */
    height: {
      control: { type: 'text' },
      description: '图表高度',
      table: {
        type: { summary: 'number | string' },
        defaultValue: { summary: '400' }
      }
    }
  }
} satisfies Meta<typeof Heatmap>

// 导出元数据
export default meta
export { meta }

/**
 * 故事对象类型
 * 基于元数据推断的故事类型
 */
type Story = StoryObj<typeof meta>

// ============================================================================
// 默认故事
// ============================================================================

/**
 * 默认基础使用示例
 * 展示组件的基本用法和基础配置
 */
const defaultData: HeatmapSeries[] = [
  {
    name: '产品销售',
    data: [
      { x: '北京', y: '1月', value: 120 },
      { x: '北京', y: '2月', value: 180 },
      { x: '北京', y: '3月', value: 220 },
      { x: '上海', y: '1月', value: 150 },
      { x: '上海', y: '2月', value: 200 },
      { x: '上海', y: '3月', value: 240 },
      { x: '广州', y: '1月', value: 100 },
      { x: '广州', y: '2月', value: 130 },
      { x: '广州', y: '3月', value: 170 }
    ]
  }
]

/**
 * 默认故事
 * 使用基础的产品销售数据，展示标准的热力图效果
 * 特点：包含网格线、图例、工具提示，但不显示数值标签
 */
export const Default: Story = {
  /**
   * 故事参数
   * 使用默认配置的数据和尺寸
   */
  args: {
    data: defaultData,
    width: 700,
    height: 400
  },

  /**
   * 故事名称
   * 在 Storybook 侧边栏中显示的名称
   */
  name: '默认示例',

  /**
   * 故事描述
   * 在文档中显示的详细说明
   */
  parameters: {
    docs: {
      description: {
        story: `
这是热力图组件的默认用法示例。

特点：
- 使用产品销售数据（北京、上海、广州三个城市在1-3月的销售情况）
- 启用网格线辅助阅读
- 启用工具提示显示详细信息
- 启用图例显示颜色映射范围
- 不显示数值标签，保持视觉简洁

适用场景：
- 基础数据展示
- 趋势分析
- 对比分析
        `.trim()
      }
    }
  }
}

// ============================================================================
// 带数值标签的故事
// ============================================================================

/**
 * 显示数值标签示例
 * 展示如何显示具体的数值
 */
export const WithValues: Story = {
  /**
   * 继承默认故事的数据和配置
   */
  args: {
    ...Default.args,

    /**
     * 启用数值标签显示
     */
    showValues: true,

    /**
     * 自定义数值格式化
     * 将数值格式化为货币格式
     */
    formatValue: (value) => `${value}万`
  },

  name: '显示数值',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了在热力图上直接显示数值的用法。

特点：
- 每个单元格都显示具体的数值
- 数值颜色会根据背景色自动调整（深色背景使用白色文字，浅色背景使用深色文字）
- 支持自定义数值格式化函数

优点：
- 精确展示数值
- 便于读取具体数据
- 适合需要精确数值的场景

注意事项：
- 数值较多时可能影响视觉美观
- 建议配合较大的单元格使用
        `.trim()
      }
    }
  }
}

// ============================================================================
// 自定义样式故事
// ============================================================================

/**
 * 自定义样式示例
 * 展示如何自定义单元格的外观
 */
export const CustomStyle: Story = {
  args: {
    ...Default.args,

    /**
     * 增大单元格间距
     * 使单元格之间有更明显的分隔
     */
    cellGap: 8,

    /**
     * 移除圆角
     * 使用直角矩形
     */
    cellRadius: 0,

    /**
     * 自定义颜色方案
     * 使用蓝色渐变色调
     */
    colorScale: [
      '#f0f9ff',  // 极浅蓝
      '#bae6fd',  // 浅蓝
      '#7dd3fc',  // 中浅蓝
      '#38bdf8',  // 中蓝
      '#0ea5e9',  // 中深蓝
      '#0284c7',  // 深蓝
      '#0369a1'   // 极深蓝
    ]
  },

  name: '自定义样式',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了如何自定义热力图的外观样式。

自定义选项：
1. **cellGap: 8** - 单元格间距设置为8像素，增强视觉分隔
2. **cellRadius: 0** - 移除圆角，使用直角矩形
3. **colorScale** - 自定义蓝色渐变配色方案

适用场景：
- 品牌主题定制
- 与现有设计系统保持一致
- 特殊的视觉需求

设计建议：
- 根据内容选择合适的颜色方案
- 保持足够的对比度
- 考虑色盲用户的可访问性
        `.trim()
      }
    }
  }
}

// ============================================================================
// 无网格故事
// ============================================================================

/**
 * 极简风格示例
 * 展示没有网格线的简洁样式
 */
export const NoGrid: Story = {
  args: {
    ...Default.args,

    /**
     * 隐藏网格线
     */
    showGrid: false,

    /**
     * 减小单元格间距
     * 营造更紧密的视觉效果
     */
    cellGap: 1
  },

  name: '无网格线',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了极简风格的热力图。

特点：
- 隐藏所有网格线
- 使用最小的单元格间距（1像素）
- 专注于数据和颜色本身

优势：
- 视觉更加简洁
- 减少视觉干扰
- 突出数据本身

适用场景：
- 艺术性展示
- 高端设计需求
- 减少视觉噪音的环境

注意事项：
- 可能会降低数据的可读性
- 建议配合数值标签使用
        `.trim()
      }
    }
  }
}

// ============================================================================
// 无图例故事
// ============================================================================

/**
 * 无图例示例
 * 展示不显示图例的热力图
 */
export const NoLegend: Story = {
  args: {
    ...Default.args,

    /**
     * 隐藏图例
     */
    showLegend: false
  },

  name: '无图例',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了不显示图例的热力图。

适用场景：
- 图表空间有限
- 用户已了解颜色映射规则
- 追求简洁的视觉设计

搭配建议：
- 使用更直观的颜色方案
- 配合数值标签使用
- 在其他地方提供图例说明

注意事项：
- 确保颜色方案本身具有解释性
- 可能需要额外的文档说明
        `.trim()
      }
    }
  }
}

// ============================================================================
// 无工具提示故事
// ============================================================================

/**
 * 无交互示例
 * 展示静态展示的热力图
 */
export const NoTooltip: Story = {
  args: {
    ...Default.args,

    /**
     * 禁用工具提示
     */
    showTooltip: false
  },

  name: '无工具提示',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了禁用交互提示的热力图。

特点：
- 完全静态，无悬停效果
- 专注于静态数据展示
- 减少性能开销

适用场景：
- 打印或导出场景
- 固定展示内容
- 不需要详细信息的场景

注意事项：
- 建议配合数值标签使用
- 考虑在外部提供详细说明
        `.trim()
      }
    }
  }
}

// ============================================================================
// 自定义颜色故事
// ============================================================================

/**
 * 暖色调配色示例
 * 展示使用暖色调的自定义配色方案
 */
export const CustomColors: Story = {
  args: {
    ...Default.args,

    /**
     * 暖色调配色方案
     * 使用红色系渐变，从浅红到深红
     */
    colorScale: [
      '#fee2e2',  // 极浅红
      '#fecaca',  // 浅红
      '#fca5a5',  // 中浅红
      '#f87171',  // 中红
      '#ef4444',  // 中深红
      '#dc2626',  // 深红
      '#b91c1c'   // 极深红
    ]
  },

  name: '自定义颜色',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了使用暖色调配色方案的自定义热力图。

配色特点：
- 使用红色系渐变
- 从浅红色到深红色的自然过渡
- 营造温暖、警示的视觉感受

适用场景：
- 危险或警告场景
- 热度、强度展示
- 积极、热情的主题

配色建议：
- 暖色调适合表示：热度、强度、积极性
- 冷色调适合表示：冷静、技术性、专业性
- 单色调适合表示：统一的指标或评分
        `.trim()
      }
    }
  }
}

// ============================================================================
// 自定义动画故事
// ============================================================================

/**
 * 快速动画示例
 * 展示较短的动画持续时间
 */
export const CustomAnimation: Story = {
  args: {
    ...Default.args,

    /**
     * 动画持续时间设为0.5秒
     * 更快的加载动画
     */
    animationDuration: 0.5
  },

  name: '自定义动画',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了自定义动画持续时间的效果。

动画配置：
- **animationDuration: 0.5** - 动画总时长0.5秒
- 单元格依次加载，每个延迟0.02秒
- 数值标签在动画结束后0.3秒内淡入

适用场景：
- 快速交互场景
- 性能敏感环境
- 用户偏好快速响应

注意事项：
- 过短的动画可能影响用户体验
- 过长的动画可能让用户等待
- 建议在0.5-2秒之间选择
        `.trim()
      }
    }
  }
}

// ============================================================================
// 大数据集故事
// ============================================================================

/**
 * 大数据集示例
 * 展示处理大量数据的热力图
 */
const largeDataset: HeatmapSeries[] = [
  {
    name: '销售数据',
    data: Array.from({ length: 60 }, (_, i) => {
      const cities = ['北京', '上海', '广州', '深圳', '杭州', '成都']
      const months = ['1月', '2月', '3月', '4月', '5月', '6月', '7月', '8月', '9月', '10月']
      return {
        x: cities[i % cities.length],
        y: months[Math.floor(i / cities.length) % months.length],
        value: Math.floor(Math.random() * 300) + 50
      }
    })
  }
]

/**
 * 大数据集故事
 * 验证组件在处理大量数据时的性能
 */
export const LargeDataset: Story = {
  args: {
    data: largeDataset,
    width: 900,
    height: 500,

    /**
     * 不显示数值标签
     * 避免视觉混乱
     */
    showValues: false,

    /**
     * 启用工具提示
     * 通过悬停查看详细信息
     */
    showTooltip: true
  },

  name: '大数据集',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了热力图处理大量数据的能力。

数据规模：
- 60个数据点
- 6个城市 × 10个月份
- 随机数值范围：50-350

性能优化：
- 关闭数值标签显示，减少渲染压力
- 使用 SVG 高效渲染
- 动画采用批量处理

使用建议：
- 大数据集建议关闭数值标签
- 启用工具提示查看详细信息
- 考虑适当增大图表尺寸
- 使用适当的颜色对比度

注意事项：
- 数据点过多可能影响可读性
- 建议在10x10范围内使用
        `.trim()
      }
    }
  }
}

// ============================================================================
// 多系列故事
// ============================================================================

/**
 * 多系列数据示例
 * 展示多个产品线的对比数据
 */
const multiSeriesData: HeatmapSeries[] = [
  {
    name: '2023年',
    data: [
      { x: '产品A', y: 'Q1', value: 120 },
      { x: '产品A', y: 'Q2', value: 180 },
      { x: '产品A', y: 'Q3', value: 220 },
      { x: '产品B', y: 'Q1', value: 90 },
      { x: '产品B', y: 'Q2', value: 150 },
      { x: '产品B', y: 'Q3', value: 200 }
    ]
  },
  {
    name: '2024年',
    data: [
      { x: '产品A', y: 'Q1', value: 150 },
      { x: '产品A', y: 'Q2', value: 200 },
      { x: '产品A', y: 'Q3', value: 250 },
      { x: '产品B', y: 'Q1', value: 110 },
      { x: '产品B', y: 'Q2', value: 170 },
      { x: '产品B', y: 'Q3', value: 230 }
    ]
  }
]

/**
 * 多系列故事
 * 展示两个时间段的对比数据
 */
export const MultiSeries: Story = {
  args: {
    data: multiSeriesData,
    width: 700,
    height: 400
  },

  name: '多系列对比',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了热力图的多系列数据支持。

数据对比：
- **2023年数据**：产品A和产品B在Q1-Q3的业绩
- **2024年数据**：同产品线的年度对比

对比维度：
- 横向对比：不同产品线在同一季度的表现
- 纵向对比：同一产品线在不同季度的增长

应用场景：
- 年度业绩对比
- 产品线分析
- 季度趋势展示

特点：
- 工具提示会显示对应的系列名称
- 颜色映射基于所有系列的整体范围
- 便于发现增长模式和异常值

注意事项：
- 系列数据应保持相同的维度结构
- 系列数量建议不超过5个
        `.trim()
      }
    }
  }
}

// ============================================================================
// 负值故事
// ============================================================================

/**
 * 负值数据示例
 * 展示包含负数值的数据可视化
 */
const negativeData: HeatmapSeries[] = [
  {
    name: '利润分析',
    data: [
      { x: '产品A', y: 'Q1', value: -50 },
      { x: '产品A', y: 'Q2', value: 30 },
      { x: '产品A', y: 'Q3', value: 80 },
      { x: '产品B', y: 'Q1', value: 20 },
      { x: '产品B', y: 'Q2', value: 60 },
      { x: '产品B', y: 'Q3', value: 100 },
      { x: '产品C', y: 'Q1', value: -30 },
      { x: '产品C', y: 'Q2', value: -10 },
      { x: '产品C', y: 'Q3', value: 40 }
    ]
  }
]

/**
 * 负值故事
 * 展示处理盈亏、增减等包含负值的数据
 */
export const NegativeValues: Story = {
  args: {
    data: negativeData,
    width: 600,
    height: 400,

    /**
     * 显示数值标签
     * 便于查看具体的盈亏数值
     */
    showValues: true
  },

  name: '负值数据',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了热力图处理负值数据的能力。

应用场景：
- **财务分析**：盈亏状况、利润波动
- **业绩对比**：增减变化、偏差分析
- **市场调研**：满意度、推荐指数（反向计分）
- **科学实验**：实验组与对照组的差值

数据特点：
- 负值和正值并存
- 需要直观的视觉区分
- 工具提示显示具体数值

设计要点：
- 使用渐变色带覆盖整个数值范围
- 负值区域和正值区域应有视觉区别
- 建议配合数值标签使用

注意事项：
- 负值的颜色选择要谨慎
- 考虑文化背景（红色在某些文化中代表危险）
- 提供清晰的图例说明
        `.trim()
      }
    }
  }
}

// ============================================================================
// 主题集成故事
// ============================================================================

/**
 * 主题桥接器集成示例
 * 展示组件与七轴主题系统的集成
 */
export const WithThemeBridge: Story = {
  args: {
    ...Default.args,

    /**
     * 启用数值标签
     * 在主题下验证文字可读性
     */
    showValues: true
  },

  /**
   * 自定义渲染函数
   * 在 ThemeBridge 容器中渲染组件
   */
  render: (args) => (
    <ThemeBridge>
      <Heatmap {...args} />
    </ThemeBridge>
  ),

  name: '主题系统集成',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了热力图组件与七轴主题系统的完整集成。

主题特性：
- **自动适配**：组件自动适应深色/浅色主题
- **变量使用**：使用 CSS 自定义属性确保一致性
- **动态切换**：支持主题的动态切换
- **可访问性**：在不同主题下保持良好的对比度

测试建议：
- 切换不同的主题配方
- 验证文字在所有主题下的可读性
- 检查图例和工具提示的显示效果

技术实现：
- 使用 \`var(--color-surface)\` 等主题变量
- 深色模式下自动调整文字颜色
- 响应式的颜色对比度调整

适用场景：
- 主题敏感的应用程序
- 需要夜间模式的应用
- 品牌定制需求
        `.trim()
      }
    }
  }
}

// ============================================================================
// 小尺寸故事
// ============================================================================

/**
 * 小尺寸示例
 * 展示紧凑布局的热力图
 */
export const Small: Story = {
  args: {
    data: defaultData,
    width: 400,
    height: 300,

    /**
     * 不显示数值标签
     * 节省空间
     */
    showValues: false
  },

  name: '小尺寸',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了小尺寸热力图的紧凑布局。

尺寸规格：
- 宽度：400px
- 高度：300px
- 适合小空间展示

应用场景：
- 仪表板组件
- 卡片内部嵌入
- 移动端展示
- 侧边栏组件

设计要点：
- 关闭数值标签节省空间
- 保持适当的单元格大小
- 使用清晰的字体大小
- 确保可点击区域足够大

注意事项：
- 小尺寸下信息密度较高
- 建议配合工具提示使用
- 考虑移动端的触摸操作
        `.trim()
      }
    }
  }
}

// ============================================================================
// 大尺寸故事
// ============================================================================

/**
 * 大尺寸示例
 * 展示高分辨率的热力图
 */
export const Large: Story = {
  args: {
    data: defaultData,
    width: 1000,
    height: 600,

    /**
     * 显示数值标签
     * 利用充足的空间展示详细信息
     */
    showValues: true
  },

  name: '大尺寸',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了大尺寸热力图的丰富展示效果。

尺寸规格：
- 宽度：1000px
- 高度：600px
- 适合大屏展示

应用场景：
- 监控大屏
- 数据分析平台
- 演示文稿
- 报表展示

优势：
- 单元格更大，数据更清晰
- 可以显示数值标签
- 更好的视觉冲击力
- 适合打印和导出

设计建议：
- 利用大尺寸展示更多细节
- 可以显示数值标签
- 考虑增加交互性
- 注意保持视觉平衡
        `.trim()
      }
    }
  }
}

// ============================================================================
// 百分比宽度故事
// ============================================================================

/**
 * 响应式宽度示例
 * 展示百分比宽度实现响应式布局
 */
export const PercentageWidth: Story = {
  args: {
    ...Default.args,

    /**
     * 使用百分比宽度
     * 实现响应式布局
     */
    width: '100%',

    /**
     * 固定高度
     */
    height: 400
  },

  name: '响应式宽度',

  parameters: {
    docs: {
      description: {
        story: `
此示例展示了热力图的响应式宽度特性。

配置特点：
- **width: '100%'** - 宽度自动适应容器
- **height: 400** - 高度保持固定
- **布局：padded** - Storybook 使用带边距的布局

优势：
- 自动适配不同屏幕尺寸
- 与响应式设计完美集成
- 灵活适应布局需求

实现机制：
- SVG 元素使用 \`width: 100%\`
- 容器自动计算实际宽度
- 单元格尺寸动态调整

使用场景：
- 响应式网页布局
- 自适应容器
- 多设备适配

注意事项：
- 需要确保容器有明确的宽度约束
- 高度不建议使用百分比（需要明确参考）
- 考虑最小和最大宽度限制
        `.trim()
      }
    },

    /**
     * 使用带边距的布局
     * 给组件留出展示空间
     */
    layout: 'padded'
  }
}
