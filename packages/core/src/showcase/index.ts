/**
 * Showcase Components - 展示组件集合
 *
 * 提供组件展示、演示、案例等组件
 * 用于展示组件库的功能和效果
 */

// 临时导出空对象，避免依赖问题
// TODO: 修复组件依赖问题后重新导出

export const ShowcaseComponents = {
  CodeDemo: null,
  Component3DCarousel: null,
  CounterAnimation: null,
  Features: null,
  StatsCard: null
} as any

// 类型导出
export interface CodeDemoProps {}
export interface Component3DCarouselProps {}
export interface CounterAnimationProps {}
export interface FeaturesProps {}
export interface StatsCardProps {}