/**
 * Showcase 组件导出 - 符合七轴主题系统 v1.4 SSOT
 *
 * 展示组件集合 - CodeDemo 等
 */

// =============================================================================
// 核心组件导出
// =============================================================================

// CodeDemo 组件系列
export {
  CodeDemo
} from './code-demo/code-demo'
export type {
  CodeDemoProps
} from './code-demo/code-demo'

// =============================================================================
// 组件集合导出
// =============================================================================

/**
 * 展示组件集合
 */
export const ShowcaseComponents = {
  CodeDemo,
} as const

// =============================================================================
// 类型导出
// =============================================================================

/**
 * 所有展示组件的 Props 类型联合
 */
export type ShowcaseComponentProps = CodeDemoProps