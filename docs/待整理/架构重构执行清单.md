# 📋 Xorigo UI 架构重构执行清单

> **基于**：[Xorigo UI 架构白皮书 v1.1.0](./Xorigo%20UI%20架构白皮书.md)
> **适用范围**：Monorepo 重构、包边界调整、工程化护栏部署
> **执行周期**：1个月（P0：1周内，P1：2周内，P2：1个月内）

---

## 🎯 执行概览

### 优先级说明
- **🔴 P0（立即执行）**：阻塞性问题，必须立即修复
- **🟡 P1（1周内）**：重要优化，影响开发体验
- **🟢 P2（1个月内）**：长期改进，提升工程质量

### 总体进度
- **P0 关键修正**：□ 0/8 完成
- **P1 重要优化**：□ 0/6 完成
- **P2 长期改进**：□ 0/10 完成

---

## 🔴 P0 关键修正（立即执行，1周内完成）

### 1. 目录去重与命名统一
**文件来源**：架构白皮书 13.1 节

```bash
# 执行命令清单
□ 1.1 备份当前结构
   cp -r packages/ packages-backup/

□ 1.2 目录迁移
   mkdir -p packages/core/src/base/
   mv packages/core/src/components/ui/* packages/core/src/base/
   rmdir packages/core/src/components/ui

   mkdir -p packages/core/src/layout/
   mv packages/core/src/components/layout/* packages/core/src/layout/
   rmdir packages/core/src/components/layout

   mkdir -p packages/core/src/navigation/
   mv packages/core/src/components/navigation/* packages/core/src/navigation/
   rmdir packages/core/src/components/navigation

□ 1.3 高级组件迁移
   mkdir -p packages/advanced/src/
   mv packages/core/src/components/advanced/* packages/advanced/src/

□ 1.4 业务组件迁移
   mkdir -p packages/composite/src/blocks/
   mv packages/core/src/blocks/* packages/composite/src/blocks/

□ 1.5 系统包迁移
   mkdir -p packages/system/src/
   mv packages/core/src/theme/* packages/system/src/theme/
   mv packages/core/src/accessibility/* packages/system/src/accessibility/

□ 1.6 适配器重组
   mkdir -p packages/core/src/adapters/radix/
   mv packages/core/src/radix/* packages/core/src/adapters/radix/
```

**验证标准**：
- [ ] 无重复目录结构
- [ ] 所有组件文件已迁移
- [ ] 导出路径正确更新

### 2. 依赖关系清理
**文件来源**：架构白皮书 13.1 节

```bash
# 包依赖更新清单
□ 2.1 更新 packages/core/package.json
   "dependencies": {
     "@xorigo-ui/tokens": "workspace:*",
     "@xorigo-ui/style-recipe": "workspace:*",
     "@xorigo-ui/system": "workspace:*"
   }

□ 2.2 更新 packages/advanced/package.json
   "dependencies": {
     "@xorigo-ui/core": "workspace:*",
     "@xorigo-ui/tokens": "workspace:*"
   }

□ 2.3 更新 packages/composite/package.json
   "dependencies": {
     "@xorigo-ui/core": "workspace:*",
     "@xorigo-ui/advanced": "workspace:*",
     "@xorigo-ui/tokens": "workspace:*"
   }
```

**验证标准**：
- [ ] 无循环依赖
- [ ] 依赖方向正确（core ← advanced ← composite）
- [ ] workspace 协议正确使用

### 3. 导出配置修正
**文件来源**：架构白皮书 13.3 节

```typescript
// packages/core/src/index.ts 导出修正
□ 3.1 更新主导出文件
   export * from './base/'
   export * from './layout/'
   export * from './navigation/'
   export * from './form/'
   export * from './data/'
   export * from './feedback/'
   export * from './adapters/'

   // 配方系统从独立包导出
   export {
     StyleRecipeProvider,
     useStyleRecipe,
     corporateBlueRecipe,
     type StyleRecipe
   } from '@xorigo-ui/style-recipe'

□ 3.2 更新各包内部导出
   // packages/core/src/base/index.ts
   export { Button } from './button'
   export { Input } from './input'
   // ... 其他基础组件
```

**验证标准**：
- [ ] 导出路径正确
- [ ] 类型导出完整
- [ ] 无循环导出

### 4. 根配置文件统一
**文件来源**：架构白皮书 16.1 节

```json
// tsconfig.json 路径映射
□ 4.1 配置 TypeScript 路径
   {
     "compilerOptions": {
       "paths": {
         "@xorigo-ui/tokens": ["./packages/tokens/src"],
         "@xorigo-ui/style-recipe": ["./packages/style-recipe/src"],
         "@xorigo-ui/system": ["./packages/system/src"],
         "@xorigo-ui/core": ["./packages/core/src"],
         "@xorigo-ui/hooks": ["./packages/hooks/src"],
         "@xorigo-cli/commands": ["./packages/cli/src"]
       }
     }
   }
```

**验证标准**：
- [ ] 路径映射正确
- [ ] IDE 智能提示正常
- [ ] 构建无路径解析错误

### 5. CI 基础检查流水线
**文件来源**：架构白皮书 15.4 节

```yaml
# .github/workflows/ci.yml 基础配置
□ 5.1 创建 CI 工作流
   name: CI
   on: [push, pull_request]
   jobs:
     quality-check:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - uses: actions/setup-node@v4
         - run: npm ci
         - run: npm run lint
         - run: npm run type-check
         - run: npm run test
```

**验证标准**：
- [ ] CI 流水线运行成功
- [ ] 所有检查项通过
- [ ] 失败时正确阻止合并

### 6. 包结构验证
**文件来源**：架构白皮书 13.2 节

```bash
# 包结构检查脚本
□ 6.1 创建验证脚本 scripts/validate-packages.js
   // 检查每个包的必要文件
   // - package.json
   // - README.md
   // - src/index.ts
   // - tests/ 目录

□ 6.2 执行验证
   node scripts/validate-packages.js
```

**验证标准**：
- [ ] 所有包结构完整
- [ ] 无缺失必要文件
- [ ] 命名规范符合要求

### 7. 依赖方向检查
**文件来源**：架构白皮书 15.2 节

```bash
# 依赖方向验证
□ 7.1 安装依赖检查工具
   npm install --save-dev madge

□ 7.2 创建依赖检查脚本
   # scripts/check-dependencies.sh
   madge --circular packages/

□ 7.3 执行检查
   npm run check-dependencies
```

**验证标准**：
- [ ] 无循环依赖
- [ ] 依赖方向符合架构
- [ ] 检查自动化

### 8. 基础测试迁移
**文件来源**：架构白皮书 8.1 节

```bash
# 测试迁移就近化
□ 8.1 移动测试文件到包内
   mkdir -p packages/core/tests/
   mv tests/* packages/core/tests/

□ 8.2 更新测试配置
   # vitest.config.js
   testInclude: ['packages/*/tests/**/*.test.{ts,tsx}']
```

**验证标准**：
- [ ] 测试文件就近放置
- [ ] 测试配置正确
- [ ] 测试可以正常运行

---

## 🟡 P1 重要优化（1-2周内完成）

### 9. CLI 最小命令集实现
**文件来源**：架构白皮书 十九章

```bash
# CLI 基础命令
□ 9.1 创建包脚手架命令
   npx @xorigo-cli create my-app

□ 9.2 创建组件生成命令
   npx @xorigo-cli add-component Button

□ 9.3 创建主题切换命令
   npx @xorigo-ui switch-theme corporate-blue
```

### 10. Registry 元数据自动化
**文件来源**：架构白皮书 6.6 节

```typescript
// 自动化组件发现和注册
□ 10.1 实现组件扫描器
   // 扫描所有包的导出组件
   // 自动生成注册表元数据

□ 10.2 创建 Registry API
   // GET /api/components - 获取组件列表
   // GET /api/components/:name - 获取组件详情
```

### 11. TypeScript 严格模式
**文件来源**：架构白皮书 16.1 节

```json
// 启用严格模式
□ 11.1 更新 tsconfig.json
   {
     "compilerOptions": {
       "strict": true,
       "noUncheckedIndexedAccess": true
     }
   }

□ 11.2 修复类型错误
   // 逐个修复严格模式下的类型问题
```

### 12. ESLint 七轴规则集
**文件来源**：架构白皮书 16.2 节

```javascript
// 自定义 ESLint 规则
□ 12.1 创建七轴规则插件
   // 检查组件是否符合九大类分类
   // 检查是否正确使用设计令牌
   // 检查可访问性实践
```

### 13. Storybook 集成
**文件来源**：架构白皮书 9.2 节

```bash
# Storybook 配置
□ 13.1 安装和配置 Storybook
   npx storybook@latest init

□ 13.2 创建组件故事
   // 为每个核心组件创建 .stories.tsx 文件
```

### 14. 文档站点生成
**文件来源**：架构白皮书 9.1 节

```bash
# VitePress 文档站点
□ 14.1 初始化文档站点
   npm install vitepress

□ 14.2 配置文档结构
   // 创建 API 文档自动生成
   // 创建使用指南页面
```

---

## 🟢 P2 长期改进（1个月内完成）

### 15-24. 完整工程化体系
**详细内容请参考架构白皮书第十五章和第十七章**

- □ 15. 覆盖层管理标准实现
- □ 16. 键盘矩阵标准实现
- □ 17. 虚拟化合约标准实现
- □ 18. 完整测试覆盖（单元+集成+E2E）
- □ 19. 性能监控体系
- □ 20. 可访问性自动化测试
- □ 21. 国际化支持
- □ 22. 多包导出优化
- □ 23. 版本发布自动化
- □ 24. 文档自动同步

---

## ✅ 验收标准

### 总体验收条件
- [ ] 所有 P0 项目完成（8/8）
- [ ] 所有 P1 项目完成（6/6）
- [ ] 核心功能测试通过率 >95%
- [ ] 构建成功率 100%
- [ ] CI/CD 流水线稳定运行

### 质量门禁
- [ ] TypeScript 编译无错误
- [ ] ESLint 检查通过
- [ ] 单元测试覆盖率 >80%
- [ ] 可访问性评分 >90分
- [ ] 性能评分 >85分

### 文档完整性
- [ ] API 文档自动生成
- [ ] 使用指南完整
- [ ] 迁移指南提供
- [ ] 贡献指南更新

---

## 📞 执行支持

### 问题反馈
- **技术问题**：在项目 Issues 中提交
- **架构疑问**：参考架构白皮书对应章节
- **实施阻塞**：联系架构组评审

### 工具推荐
- **依赖检查**：`madge` - 检查循环依赖
- **包分析**：`npm analyze` - 分析包大小
- **类型检查**：`tsc --noEmit` - 严格类型检查
- **测试覆盖**：`vitest --coverage` - 测试覆盖率

### 进度跟踪
```bash
# 检查当前进度
npm run check-progress

# 生成进度报告
npm run report-progress
```

---

**📅 执行计划**：
- **第1周**：完成所有 P0 关键修正
- **第2周**：完成 P1 重要优化
- **第3-4周**：完成 P2 长期改进
- **第5周**：最终验收和发布准备

**🎯 成功标准**：重构后的 Xorigo UI 具备清晰的包边界、完善的工程化护栏、高质量的代码标准和完整的文档体系。